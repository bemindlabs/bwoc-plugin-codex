---
name: bwoc-{{VERB}}
description: {{DESC}}
---

# bwoc-{{VERB}}

{{SUMMARY}}

Thin wrapper over the `bwoc` CLI:

```bash
{{USAGE}}
```

{{BODY}}

This is a thin wrapper over `bwoc {{VERB}}` — no business logic. Pass user-supplied text quoted so shell metacharacters are not interpreted, and relay the CLI output back to the user.
